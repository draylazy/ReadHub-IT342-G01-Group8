# ===============================
# DATABASE CONFIGURATION
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/readhub_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=171717

# spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================
# JWT CONFIGURATION
# ===============================
# The code is asking for 'app.jwt.secret', so we provide it here:
app.jwt.secret=ThisIsANewVeryLongAndVerySecureSecretKeyForReadHubHS512abcdef123
app.jwt.expiration-ms=86400000

# Keeping the camelCase version just in case other parts of your code need it
app.jwtSecret=ThisIsANewVeryLongAndVerySecureSecretKeyForReadHubHS512abcdef123
app.jwtExpirationMs=86400000

# ===============================
# SERVER CONFIGURATION
# ===============================
server.port=8080
server.error.include-message=always

# ===============================
# EMAIL CONFIGURATION (Gmail)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Replace with YOUR Gmail address
spring.mail.username=rivazendrix@gmail.com
# Replace with the 16-char APP PASSWORD (not your login password)
spring.mail.password=cejl khbc wzph pmmg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true